<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:e="clr-namespace:BinaryStudio.Modeling.PlatformUI.Controls">
  <Style TargetType="{x:Type e:XYViewport}">
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Margin" Value="10"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type e:XYViewport}">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
            <Grid>
              <ScrollViewer Background="Transparent" CanContentScroll="True" x:Name="ScrollViewer"
                            HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                            AllowDrop="False">
                <Grid>
                  <e:XYViewportSurface Offset="{Binding ElementName=ItemsHost,Path=Offset,Mode=OneWay}" x:Name="ViewportSurface"/>
                  <e:XYViewportPanel x:Name="ItemsHost" IsItemsHost="True">
                    <e:XYViewportPanel.LayoutTransform>
                      <ScaleTransform ScaleX="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Scale,Mode=OneWay}"
                                    ScaleY="{Binding RelativeSource={RelativeSource TemplatedParent},Path=Scale,Mode=OneWay}"/>
                    </e:XYViewportPanel.LayoutTransform>
                  </e:XYViewportPanel>
                </Grid>
              </ScrollViewer>
              <e:BridgeReference Source="{Binding ElementName=ScrollViewer,Path=HorizontalOffset,Mode=OneWay}" Target="{Binding RelativeSource={RelativeSource TemplatedParent},Path=HorizontalOffset,Mode=OneWayToSource}"/>
              <e:BridgeReference Source="{Binding ElementName=ScrollViewer,Path=VerticalOffset,Mode=OneWay}" Target="{Binding RelativeSource={RelativeSource TemplatedParent},Path=VerticalOffset,Mode=OneWayToSource}"/>
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="{x:Type e:XYViewportShape}">
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Background"  Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Margin" Value="0"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type e:XYViewportShape}">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
            <ContentPresenter Content="{TemplateBinding Content}"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style TargetType="{x:Type e:XYViewportShapeSizeDecorator}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type e:XYViewportShapeSizeDecorator}">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
            <Grid Margin="-7,-7,-9,-9">
              <Grid.Resources>
                <Style TargetType="{x:Type e:Thumb}">
                  <Setter Property="BorderThickness" Value="0"/>
                  <Setter Property="BorderBrush" Value="{x:Null}"/>
                  <Setter Property="Background"  Value="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"/>
                  <Setter Property="SnapsToDevicePixels" Value="True"/>
                  <Setter Property="Width"  Value="7"/>
                  <Setter Property="Height" Value="7"/>
                  <Setter Property="Template">
                    <Setter.Value>
                      <ControlTemplate TargetType="{x:Type e:Thumb}">
                        <Rectangle StrokeThickness="{TemplateBinding BorderThickness}"
                              Stroke="{TemplateBinding BorderBrush}"
                              Fill="{TemplateBinding Background}"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                      </ControlTemplate>
                    </Setter.Value>
                  </Setter>
                </Style>
              </Grid.Resources>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>
              <Rectangle Fill="Transparent" Margin="3" Grid.Column="0" Grid.Row="0" Grid.RowSpan="5" Grid.ColumnSpan="5"
                         Stroke="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"
                         StrokeThickness="1" StrokeDashArray="4 2"/>
              <e:Thumb Grid.Row="0" Grid.Column="0" Cursor="SizeNWSE" x:Name="ThumbLeftTop"/>
              <e:Thumb Grid.Row="0" Grid.Column="4" Cursor="SizeNESW" x:Name="ThumbRightTop"/>
              <e:Thumb Grid.Row="4" Grid.Column="0" Cursor="SizeNESW" x:Name="ThumbLeftBottom"/>
              <e:Thumb Grid.Row="4" Grid.Column="4" Cursor="SizeNWSE" x:Name="ThumbRightBottom"/>
              <e:Thumb Grid.Row="2" Grid.Column="0" Cursor="SizeWE"   x:Name="ThumbLeft"/>
              <e:Thumb Grid.Row="2" Grid.Column="4" Cursor="SizeWE"   x:Name="ThumbRight"/>
              <e:Thumb Grid.Row="0" Grid.Column="2" Cursor="SizeNS"   x:Name="ThumbTop"/>
              <e:Thumb Grid.Row="4" Grid.Column="2" Cursor="SizeNS"   x:Name="ThumbBottom"/>
              <e:Thumb Grid.Row="0" Grid.Column="3" Cursor="SizeAll"  x:Name="ThumbAll" Background="Red"/>
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>